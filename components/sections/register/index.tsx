import RegisterForm from "@/components/form/RegisterForm";

const RegisterSection = () => {
  return (
    <div className="bg-gradient-to-r from-purple-700 to-purple-500 w-full min-h-screen">
      <RegisterForm />
    </div>
  );
};

export default RegisterSection;
